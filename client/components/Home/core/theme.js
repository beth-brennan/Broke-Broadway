import { DefaultTheme } from 'react-native-paper'

export const theme = {
  ...DefaultTheme,
  roundness: 2,
  colors: {
    ...DefaultTheme.colors,
    primary: '#52154e',
    secondary: '#111344',
    tertiary: '#F9CFF2'
  },
};
